模仿/model,/handler,/service,/router,/dao下的user写法风格（不需要完全相同的模仿），为location补充内容,可参考/model/mysql/advanced/locations.sql

我需要你完成的每一个功能应当：在router中注册，通过handler解析参数，在service里处理业务逻辑，并在dao完成（可能复用的）数据库操作
当对代码部分不清楚时，有必要递归部分项目代码以确保完整性
可在response_code.go中定义必要的错误返回代码和消息
鉴权已经写在router的use（middleware）
应当在Handler上方写swagger注释，可适当自由发挥
所有疑问或棘手的问题向用户确认，不得自己乱写

location简单介绍：这是一个地点管理，mysql的locations中存放了多颗树，根节点的父亲是0，深度任意

public
1, GET "/item/{itemID}/locations" 我们还没做item相关，所以你先var一个locationid占位，用户后续自己补充通过itemid获取locationid。返回json格式的一个[]Location,元素应当从递归得到的父节点到子节点排序

private

admin
1, POST "/location/create" 创建一个location，已经写好请求体
2, POST "/location/update" 给定ID和createRequest中的变量，完成所有修改（无item等约束），并非整体替换
3, DELETE "/location/{id}" 硬删除ID；若有子元素则报错